<?php

$input = [1, 2, 3, 5, 4, 6, 8, 9, 9, 10];
$output = [];

// Split the input every time the number doesn't increase
// The result should be an array of arrays, for which each array only has increasing numbers and is never empty

// Code here

// End

var_dump( $output );
