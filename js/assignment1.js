module.exports = {
    /**
     * split the input every time the number doesn't increase
     *
     * @param input The input array of numbers
     * @return The result should be an array of arrays, for which each array only has increasing numbers and is never empty
     */
    splitIncreasing: function (input) {
        // TODO
    }
};
